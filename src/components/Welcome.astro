---
import Button from "./shared/Button.astro";
import Logo from "./shared/Logo.astro";

import en from '../i18n/messages/en.json';
import es from '../i18n/messages/es.json';

const lang = Astro.url.pathname.includes('/es') ? 'es' : 'en';
const t = lang === 'es' ? es : en;
const base = lang === 'es' ? '/es' : '';
const exploreLabel = lang === 'es' ? 'Explorar nuestro trabajo' : 'Explore Our Work';
---

<section class="relative w-full h-screen overflow-hidden flex items-center justify-start text-left pt-16" id="hero">
  <!-- Video de fondo -->
  <video
    autoplay
    muted
    loop
    playsinline
    class="absolute top-0 left-0 w-full h-full object-cover z-0"
  >
    <source src="/assets/videos/hero.mp4" type="video/mp4" />
    Tu navegador no soporta el video HTML5.
  </video>

  <!-- Overlay -->
  <div class="absolute inset-0 bg-taupe bg-opacity-50 z-2"></div>

  <!-- Contenido -->
  <div class="relative z-3 text-white px-6 md:px-12 lg:px-16 max-w-screen-xl mx-auto w-full">
    <div class="max-w-2xl">
      <Logo size="hero" as="h1" class="mb-6" />

      <p class="text-2xl md:text-3xl lg:text-4xl font-light mb-8 leading-relaxed">
        {t.hero.title}
      </p>

      <div class="text-base md:text-lg font-light text-white/80 mb-10 space-y-2">
        <p>Moody, cinematic, and deeply atmospheric.</p>
        <p>Weddings with soul & documentary eye.</p>
        <p>Old film tones — Raw emotions</p>
      </div>

      <Button href={`${base}/gallery`} label={exploreLabel} variant="tertiary" size="compact" />
    </div>
  </div>
</section>
